tmp
.DS_Store
.claude
node_modules/
package-lock.json
stocks/data/raw/

# Secrets live in .env; never commit them (the deploy script also refuses to copy it).
.env
.env.*
!.env.example

# npm install output
node_modules/

# Server-side job state (stocks/refresh-on-server.sh, PM2 logs)
logs/
.last-refresh-stats.json
.refresh.lock
