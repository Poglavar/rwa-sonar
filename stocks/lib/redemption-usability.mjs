// ESM bridge over the dependency-free UMD model used by stocks.html and Node builders.
import model from './redemption-usability.js';

export const {
    REDEMPTION_EVIDENCE_STATES,
    scopeRedemptionTerm,
    shapeRedemptionUsability
} = model;
